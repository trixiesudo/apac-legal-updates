window.APAC_LEGAL_UPDATES_CONFIG = {
  apiBaseUrl: window.location.hostname.endsWith('github.io')
  ? 'https://apac-legal-updates.onrender.com'
  : 'http://127.0.0.1:8000',
  publicMode: window.location.hostname.endsWith('github.io'),
};
